// Local Sonic configuration
// Copy to 'sonic.local.js' (excluded from git) and set your preferred settings.

const options = {
  showNotifications: false
}

const server = {
  host: '0.0.0.0',
  port: 8000,
  liveReload: false
}

module.exports = { options, server }
